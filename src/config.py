from types import SimpleNamespace

config = SimpleNamespace(
    n_regs = 8,
    n_mem = 8,
    n_val = 16,
    batch_size = 1,
    intermidate_dim = 1024,
    max_tokens = 100,
    dropout = 0.8,
    prog_max_length = 16

)